import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { ThemeProvider } from '@/components/theme-provider';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TechSupport - Expert IT Solutions for Your Business',
  description: 'We provide comprehensive IT support and solutions to help your business thrive in the digital age. From network infrastructure to cloud services, we\'ve got you covered.',
  keywords: 'IT support, tech support, network solutions, cloud services, cybersecurity, IT consulting',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
